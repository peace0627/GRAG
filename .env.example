# =============================================================================
# GraphRAG Project Configuration (.env)
# =============================================================================
# 複製此文件到 .env 並填入實際值

# =============================================================================
# Knowledge Graph Database (Neo4j) - 用於存儲知識圖譜和實體關係
# =============================================================================
# Neo4j 數據庫連接 URI
NEO4J_URI=neo4j://localhost:7687
# Neo4j 用戶名 (預設: neo4j)
NEO4J_USER=neo4j
# Neo4j 密碼
NEO4J_PASSWORD=password123

# =============================================================================
# Vector Database (Supabase pgvector) - 用於向量搜索和語義匹配
# =============================================================================
# Supabase 項目 URL
SUPABASE_URL=https://your-project-url.supabase.co
# Supabase 匿名/公開金鑰
SUPABASE_KEY=your-supabase-anon-key

# =============================================================================
# Vision Language Model (VLM) - 用於多模態文檔分析和圖表理解
# =============================================================================
# OpenAI GPT-4V API 金鑰 (推薦用於高品質視覺分析)
OPENAI_API_KEY=your-openai-api-key
# Qwen2VL API 端點 URL (阿里雲兼容接口)
QWEN2VL_BASE_URL=https://api.qwen2vl.com
# Qwen2VL API 金鑰
QWEN2VL_API_KEY=your-qwen2vl-api-key
# Ollama 本地VLM服務 (可選，預設 localhost:11434)
OLLAMA_BASE_URL=http://localhost:11434/v1
OLLAMA_API_KEY=ollama
OLLAMA_MODEL=qwen3-vl:235b-cloud                    # 常用的視覺模型: llava, moondream, etc

# =============================================================================
# Language Models (LLM) - 用於文本生成和推理
# =============================================================================
# LLM 供應商: openai, ollama, vllm, lmstudio, custom
LLM_PROVIDER=openai
# 默認 LLM 模型
LLM_MODEL=gpt-4o-mini
# LLM 溫度參數 (0.0-1.0, 越高越創意)
LLM_TEMPERATURE=0.1
# LLM 最大輸出 token 數
LLM_MAX_TOKENS=2000
# 自定義 LLM API 端點 (用於 OpenAI 兼容服務)
LLM_BASE_URL=https://api.openai.com/v1

# Agent 專用模型配置
PLANNER_LLM_MODEL=gpt-4o-mini      # 查詢規劃用模型
REASONER_LLM_MODEL=gpt-4o-mini     # 推理分析用模型
ANSWERER_LLM_MODEL=gpt-4           # 最終回答生成用模型
QUERY_PARSER_LLM_MODEL=gpt-4o      # 查詢解析用模型

# 特殊溫度設置
QUERY_PARSER_TEMPERATURE=0.1       # 查詢解析需要高一致性
ANSWERER_TEMPERATURE=0.3           # 回答生成需要適度創意

# =============================================================================
# Application Settings - 應用程序通用設置
# =============================================================================
# 調試模式 (開發環境: 1, 生產環境: 0)
DEBUG=1
# 知識區段 ID (用於知識隔離)
KNOWLEDGE_AREA_ID=default_area
# 日誌級別 (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# =============================================================================
# Processing Configuration - 文檔處理參數設定
# =============================================================================
# 文本分塊參數
CHUNK_SIZE=512
CHUNK_OVERLAP=50
INCLUDE_VISUAL_CHUNKS=True

# 嵌入模型供應商配置
EMBEDDING_PROVIDER=sentence_transformers       # sentence_transformers|openai|cohere|huggingface
EMBEDDING_API_KEY=your-api-key                 # 雲端供應商的API key
EMBEDDING_BASE_URL=https://api.openai.com/v1   # 自定義API endpoint (選用)
EMBEDDING_MODEL=all-MiniLM-L6-v2              # 模型名稱
EMBEDDING_DIMENSION=384                        # 向量維度 (⚠️ 注意: 此設定不會自動更新 Supabase 向量表結構, 詳見 infrastructure/supabase/README.md)

# 知識提取參數
EXTRACT_ENTITIES=True
EXTRACT_RELATIONS=True
EXTRACT_EVENTS=True
MIN_ENTITY_CONFIDENCE=0.5

# =============================================================================
# File Upload Settings - 文件上傳相關設置
# =============================================================================
# 上傳文檔的基本路徑
UPLOAD_BASE_PATH=./uploads
# 上傳文件的最大大小 (MB)
MAX_FILE_SIZE_MB=50
